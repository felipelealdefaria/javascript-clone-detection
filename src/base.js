

function fibonacciOne(n) {
  for (var fibonacci = [0, 1], i = 1; i < n; i++) fibonacci.push(fibonacci[i] + fibonacci[i - 1]);
  return fibonacci;
}

function fibonacciTwo() {
  var fib = [];
  fib[0] = 0;
  fib[1] = 1;
  for (var i = 2; i <= 10; i++) {
    fib[i] = fib[i - 2] + fib[i - 1];
    console.log(fib[i]);
  }
}

const arrowFunction = value => {
  const {type} = value;
  return type;
};

function regularFunction(value) {
  const {type} = value;
  return type;
}

const arrowFour = (op, valueOne, valueTwo) => {
  if (op === "sub") {
    return valueOne - valueTwo;
  } else {
    return valueOne + valueTwo;
  }
};

function regularFour(operation, firstValue, secondValue) {
  switch (operation) {
    case "sub":
      return firstValue - secondValue;
    default:
      return firstValue + secondValue;
  }
}

const arrowThree = (state, value) => {
  if (state) {
    return value * 3.14;
  }
};

function regularThree(state, value) {
  const pi = 3.14;
  if (state) return value * pi;
}

const arrowTwo = (op, valueOne, valueTwo) => {
  if (op === "sum") {
    return valueOne + valueTwo;
  } else {
    return valueOne - valueTwo;
  }
};

function regularTwo(state, vOne, vTwo) {
  if (state === "sum") {
    return vOne + vTwo;
  } else {
    return vOne - vTwo;
  }
}

const arrowFunction = value => {
  const {type} = value;
  return type;
};

function regularFunction(value) {
  const {type} = value;
  return type;
}

const arrowFour = (op, valueOne, valueTwo) => {
  if (op === "sub") {
    return valueOne - valueTwo;
  } else {
    return valueOne + valueTwo;
  }
};

function regularFour(operation, firstValue, secondValue) {
  switch (operation) {
    case "sub":
      return firstValue - secondValue;
    default:
      return firstValue + secondValue;
  }
}

const arrowThree = (state, value) => {
  if (state) {
    return value * 3.14;
  }
};

function regularThree(state, value) {
  const pi = 3.14;
  if (state) return value * pi;
}

const arrowTwo = (op, valueOne, valueTwo) => {
  if (op === "sum") {
    return valueOne + valueTwo;
  } else {
    return valueOne - valueTwo;
  }
};

function regularTwo(state, vOne, vTwo) {
  if (state === "sum") {
    return vOne + vTwo;
  } else {
    return vOne - vTwo;
  }
}

function fibonacciOne(n) {
  for (var fibonacci = [0, 1], i = 1; i < n; i++) fibonacci.push(fibonacci[i] + fibonacci[i - 1]);
  return fibonacci;
}

function fibonacciTwo() {
  var fib = [];
  fib[0] = 0;
  fib[1] = 1;
  for (var i = 2; i <= 10; i++) {
    fib[i] = fib[i - 2] + fib[i - 1];
    console.log(fib[i]);
  }
}

const arrowFunction = value => {
  const {type} = value;
  return type;
};

function regularFunction(value) {
  const {type} = value;
  return type;
}

const arrowFour = (op, valueOne, valueTwo) => {
  if (op === "sub") {
    return valueOne - valueTwo;
  } else {
    return valueOne + valueTwo;
  }
};

function regularFour(operation, firstValue, secondValue) {
  switch (operation) {
    case "sub":
      return firstValue - secondValue;
    default:
      return firstValue + secondValue;
  }
}

const arrowThree = (state, value) => {
  if (state) {
    return value * 3.14;
  }
};

function regularThree(state, value) {
  const pi = 3.14;
  if (state) return value * pi;
}

const arrowTwo = (op, valueOne, valueTwo) => {
  if (op === "sum") {
    return valueOne + valueTwo;
  } else {
    return valueOne - valueTwo;
  }
};

function regularTwo(state, vOne, vTwo) {
  if (state === "sum") {
    return vOne + vTwo;
  } else {
    return vOne - vTwo;
  }
}

const arrowFunction = value => {
  const {type} = value;
  return type;
};

function regularFunction(value) {
  const {type} = value;
  return type;
}

const arrowFour = (op, valueOne, valueTwo) => {
  if (op === "sub") {
    return valueOne - valueTwo;
  } else {
    return valueOne + valueTwo;
  }
};

function regularFour(operation, firstValue, secondValue) {
  switch (operation) {
    case "sub":
      return firstValue - secondValue;
    default:
      return firstValue + secondValue;
  }
}

const arrowThree = (state, value) => {
  if (state) {
    return value * 3.14;
  }
};

function regularThree(state, value) {
  const pi = 3.14;
  if (state) return value * pi;
}

const arrowTwo = (op, valueOne, valueTwo) => {
  if (op === "sum") {
    return valueOne + valueTwo;
  } else {
    return valueOne - valueTwo;
  }
};

function regularTwo(state, vOne, vTwo) {
  if (state === "sum") {
    return vOne + vTwo;
  } else {
    return vOne - vTwo;
  }
}

function fibonacciOne(n) {
  for (var fibonacci = [0, 1], i = 1; i < n; i++) fibonacci.push(fibonacci[i] + fibonacci[i - 1]);
  return fibonacci;
}

function fibonacciTwo() {
  var fib = [];
  fib[0] = 0;
  fib[1] = 1;
  for (var i = 2; i <= 10; i++) {
    fib[i] = fib[i - 2] + fib[i - 1];
    console.log(fib[i]);
  }
}

const arrowFunction = value => {
  const {type} = value;
  return type;
};

function regularFunction(value) {
  const {type} = value;
  return type;
}

const arrowFour = (op, valueOne, valueTwo) => {
  if (op === "sub") {
    return valueOne - valueTwo;
  } else {
    return valueOne + valueTwo;
  }
};

function regularFour(operation, firstValue, secondValue) {
  switch (operation) {
    case "sub":
      return firstValue - secondValue;
    default:
      return firstValue + secondValue;
  }
}

const arrowThree = (state, value) => {
  if (state) {
    return value * 3.14;
  }
};

function regularThree(state, value) {
  const pi = 3.14;
  if (state) return value * pi;
}

const arrowTwo = (op, valueOne, valueTwo) => {
  if (op === "sum") {
    return valueOne + valueTwo;
  } else {
    return valueOne - valueTwo;
  }
};

function regularTwo(state, vOne, vTwo) {
  if (state === "sum") {
    return vOne + vTwo;
  } else {
    return vOne - vTwo;
  }
}

const arrowFunction = value => {
  const {type} = value;
  return type;
};

function regularFunction(value) {
  const {type} = value;
  return type;
}

const arrowFour = (op, valueOne, valueTwo) => {
  if (op === "sub") {
    return valueOne - valueTwo;
  } else {
    return valueOne + valueTwo;
  }
};

function regularFour(operation, firstValue, secondValue) {
  switch (operation) {
    case "sub":
      return firstValue - secondValue;
    default:
      return firstValue + secondValue;
  }
}

const arrowThree = (state, value) => {
  if (state) {
    return value * 3.14;
  }
};

function regularThree(state, value) {
  const pi = 3.14;
  if (state) return value * pi;
}

const arrowTwo = (op, valueOne, valueTwo) => {
  if (op === "sum") {
    return valueOne + valueTwo;
  } else {
    return valueOne - valueTwo;
  }
};

function regularTwo(state, vOne, vTwo) {
  if (state === "sum") {
    return vOne + vTwo;
  } else {
    return vOne - vTwo;
  }
}

function fibonacciOne(n) {
  for (var fibonacci = [0, 1], i = 1; i < n; i++) fibonacci.push(fibonacci[i] + fibonacci[i - 1]);
  return fibonacci;
}

function fibonacciTwo() {
  var fib = [];
  fib[0] = 0;
  fib[1] = 1;
  for (var i = 2; i <= 10; i++) {
    fib[i] = fib[i - 2] + fib[i - 1];
    console.log(fib[i]);
  }
}

const arrowFunction = value => {
  const {type} = value;
  return type;
};

function regularFunction(value) {
  const {type} = value;
  return type;
}

const arrowFour = (op, valueOne, valueTwo) => {
  if (op === "sub") {
    return valueOne - valueTwo;
  } else {
    return valueOne + valueTwo;
  }
};

function regularFour(operation, firstValue, secondValue) {
  switch (operation) {
    case "sub":
      return firstValue - secondValue;
    default:
      return firstValue + secondValue;
  }
}

const arrowThree = (state, value) => {
  if (state) {
    return value * 3.14;
  }
};

function regularThree(state, value) {
  const pi = 3.14;
  if (state) return value * pi;
}

const arrowTwo = (op, valueOne, valueTwo) => {
  if (op === "sum") {
    return valueOne + valueTwo;
  } else {
    return valueOne - valueTwo;
  }
};

function regularTwo(state, vOne, vTwo) {
  if (state === "sum") {
    return vOne + vTwo;
  } else {
    return vOne - vTwo;
  }
}

const arrowFunction = value => {
  const {type} = value;
  return type;
};

function regularFunction(value) {
  const {type} = value;
  return type;
}

const arrowFour = (op, valueOne, valueTwo) => {
  if (op === "sub") {
    return valueOne - valueTwo;
  } else {
    return valueOne + valueTwo;
  }
};

function regularFour(operation, firstValue, secondValue) {
  switch (operation) {
    case "sub":
      return firstValue - secondValue;
    default:
      return firstValue + secondValue;
  }
}

const arrowThree = (state, value) => {
  if (state) {
    return value * 3.14;
  }
};

function regularThree(state, value) {
  const pi = 3.14;
  if (state) return value * pi;
}

const arrowTwo = (op, valueOne, valueTwo) => {
  if (op === "sum") {
    return valueOne + valueTwo;
  } else {
    return valueOne - valueTwo;
  }
};

function regularTwo(state, vOne, vTwo) {
  if (state === "sum") {
    return vOne + vTwo;
  } else {
    return vOne - vTwo;
  }
}